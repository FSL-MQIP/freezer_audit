{
  "gitSyncId": "6697bf2009e984291321180c_6f7d785a-f81c-4650-aee0-5e2a78969011",
  "id": "Box Modification_Range_Populate",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "INSERT INTO rangeHelper (slot, designation, number)\n\tSELECT\n\t\tslot,\n\t\t{{Range_Designation.selectedOptionValue}},\n\t\t{{Range_Number.text}} - 1 + ROW_NUMBER() OVER (ORDER BY slot)\n\tFROM slotsList\n\tWHERE slot BETWEEN {{Range_Start.text}} AND {{Range_End.text}};\n\t\n\tINSERT IGNORE INTO isolates (designation, number)\n\t\tSELECT designation, number FROM rangeHelper;\n\t\t\nUPDATE slots\n\tSET\n\t\tisolate = (CASE slot\n\t\t\tWHEN 1 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =1)\n\t\t\tWHEN 2 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =2)\n\t\t\tWHEN 3 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =3)\n\t\t\tWHEN 4 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =4)\n\t\t\tWHEN 5 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =5)\n\t\t\tWHEN 6 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =6)\n\t\t\tWHEN 7 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =7)\n\t\t\tWHEN 8 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =8)\n\t\t\tWHEN 9 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =9)\n\t\t\tWHEN 10 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =10)\n\t\t\tWHEN 11 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =11)\n\t\t\tWHEN 12 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =12)\n\t\t\tWHEN 13 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =13)\n\t\t\tWHEN 14 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =14)\n\t\t\tWHEN 15 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =15)\n\t\t\tWHEN 16 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =16)\n\t\t\tWHEN 17 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =17)\n\t\t\tWHEN 18 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =18)\n\t\t\tWHEN 19 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =19)\n\t\t\tWHEN 20 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =20)\n\t\t\tWHEN 21 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =21)\n\t\t\tWHEN 22 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =22)\n\t\t\tWHEN 23 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =23)\n\t\t\tWHEN 24 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =24)\n\t\t\tWHEN 25 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =25)\n\t\t\tWHEN 26 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =26)\n\t\t\tWHEN 27 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =27)\n\t\t\tWHEN 28 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =28)\n\t\t\tWHEN 29 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =29)\n\t\t\tWHEN 30 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =30)\n\t\t\tWHEN 31 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =31)\n\t\t\tWHEN 32 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =32)\n\t\t\tWHEN 33 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =33)\n\t\t\tWHEN 34 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =34)\n\t\t\tWHEN 35 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =35)\n\t\t\tWHEN 36 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =36)\n\t\t\tWHEN 37 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =37)\n\t\t\tWHEN 38 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =38)\n\t\t\tWHEN 39 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =39)\n\t\t\tWHEN 40 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =40)\n\t\t\tWHEN 41 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =41)\n\t\t\tWHEN 42 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =42)\n\t\t\tWHEN 43 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =43)\n\t\t\tWHEN 44 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =44)\n\t\t\tWHEN 45 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =45)\n\t\t\tWHEN 46 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =46)\n\t\t\tWHEN 47 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =47)\n\t\t\tWHEN 48 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =48)\n\t\t\tWHEN 49 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =49)\n\t\t\tWHEN 50 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =50)\n\t\t\tWHEN 51 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =51)\n\t\t\tWHEN 52 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =52)\n\t\t\tWHEN 53 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =53)\n\t\t\tWHEN 54 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =54)\n\t\t\tWHEN 55 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =55)\n\t\t\tWHEN 56 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =56)\n\t\t\tWHEN 57 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =57)\n\t\t\tWHEN 58 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =58)\n\t\t\tWHEN 59 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =59)\n\t\t\tWHEN 60 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =60)\n\t\t\tWHEN 61 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =61)\n\t\t\tWHEN 62 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =62)\n\t\t\tWHEN 63 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =63)\n\t\t\tWHEN 64 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =64)\n\t\t\tWHEN 65 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =65)\n\t\t\tWHEN 66 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =66)\n\t\t\tWHEN 67 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =67)\n\t\t\tWHEN 68 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =68)\n\t\t\tWHEN 69 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =69)\n\t\t\tWHEN 70 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =70)\n\t\t\tWHEN 71 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =71)\n\t\t\tWHEN 72 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =72)\n\t\t\tWHEN 73 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =73)\n\t\t\tWHEN 74 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =74)\n\t\t\tWHEN 75 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =75)\n\t\t\tWHEN 76 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =76)\n\t\t\tWHEN 77 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =77)\n\t\t\tWHEN 78 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =78)\n\t\t\tWHEN 79 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =79)\n\t\t\tWHEN 80 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =80)\n\t\t\tWHEN 81 THEN (SELECT isolates.ID FROM isolates LEFT JOIN rangeHelper ON isolates.designation = rangeHelper.designation AND isolates.number = rangeHelper.number WHERE rangeHelper.slot =81) \n\t\t\tEND)\n\tWHERE\n\t\tbox = (SELECT ID from boxes WHERE tower = (SELECT ID FROM towers WHERE freezer = {{Freezer_Select_Select.selectedOptionValue}} AND tower = {{appsmith.store.Current_Tower}}) AND box = {{appsmith.store.Current_Box}}) AND\n\t\tslot IN (SELECT slot FROM rangeHelper);\n\t\t\nDELETE FROM rangeHelper;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Freezer Database",
      "isAutoGenerated": false,
      "name": "Freezer Database",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "Range_Populate",
    "pageId": "Box Modification",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}